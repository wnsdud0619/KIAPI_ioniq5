# KIAPI_ioniq5

## 설치 (Installation)

1. **필수 라이브러리 설치** SHP 파일을 파싱하기 위해 `geopandas` 및 관련 라이브러리가 필요합니다.

   ```bash
   sudo apt update
   sudo apt install python3-geopandas python3-shapely python3-numpy python3-yaml
